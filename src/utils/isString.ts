/**
 * Функция проверяет, является ли входное значение строкой или нет.
 * @param {unknown} value - Входное значение, которое нужно проверить, является ли оно строкой
 */
export const isString = (value: unknown) => typeof value === 'string';
